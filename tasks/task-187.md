### Task 187: Bag opens on click; inventory border + padding; padding cleanup

- [x] Bag flap rotates -22° around its top hinge with a 0.35s transition when the inventory is open; reveals a darker interior shape
- [x] Inventory panel padding fully removed previously, then a uniform 4px (`p-1`) reapplied with the new bordered look
- [x] Inventory panel gets a thin rounded border (`border-white/20 rounded`) when open
- Location: `src/components/garden/article-inventory.tsx`
