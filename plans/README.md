# Implementation Plans — mannan20 Audit Remediation

Generated by a full-repo audit on 2026-07-04 (commit `04dbc8e`). Execute in priority order (P0 → P1 → P2 → P3) unless a dependency note below says otherwise. Each executor: read `PLAN.md` first for the ground rules, then the plan file fully before starting, honor its STOP conditions, and update your row here when done. Full narrative report with evidence: https://claude.ai/code/artifact/7bb37a5f-a449-46ba-8bf6-c330a96d4f37

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## P0 — fix now (live security exposure)

| Plan | Finding IDs | Title | Effort | Depends on | Status |
|------|-------------|-------|--------|------------|--------|
| [001](001-tts-command-injection.md) | SEC-01 | Delete/lock down the unauthenticated command injection in `/api/tts` | S | — | DONE |
| [002](002-jordan-server-side-auth.md) | SEC-02, CORRECTNESS-02, TEST-01 | Give `/api/jordan/*` real server-side session auth | M | — | TODO |
| [003](003-dependency-cve-upgrade.md) | SEC-03, DEP-07 | Update Next.js + PostCSS off versions with active CVEs | S | — | BLOCKED (next fully fixed 15.5.20; postcss advisory persists via nested `@tailwindcss/postcss`/`next` copies not covered by `bun update postcss` — STOP condition, see `tasks/task-265.md`) |

## P1 — high leverage

| Plan | Finding IDs | Title | Effort | Depends on | Status |
|------|-------------|-------|--------|------------|--------|
| [004](004-auth-token-double-redeem-race.md) | CORRECTNESS-01 | Fix magic-token / site-session-code double-redeem race | S | — | TODO |
| [005](005-repo-identity-docs-refresh.md) | DOCS-02, DX-03, DX-06 | Rewrite README, retire the dead git hook, fix `.vscode/` | M | — | DONE |
| — | SEC-08 | Add CSRF state/nonce to the login callback | M | — | TODO |
| — | SEC-09 | Make sign-out actually invalidate sessions server-side | M | — | TODO |
| — | SEC-05 | Require a real gameplay-proof token before a leaderboard score counts | M | — | TODO |
| — | CORRECTNESS-04 | Dedupe leaderboard "Top 10" by owner | M | — | TODO |
| — | CORRECTNESS-06 | Fix leaderboard rename-hop-limit misattribution | M | — | TODO |
| — | TEST-02 | Root-cause `products-gallery`/`garden-health-article` 100%/89% e2e failures | S (investigate) | — | TODO |
| — | TEST-03 | Decide: enable `CONTACT_CHALLENGE` or skip its 13 dependent tests | S | needs Mannan's call | TODO |
| — | DEBT-01 | Consolidate 15 independent Redis client instantiations into one module | S | — | TODO |
| — | DEBT-03, PERF-02 | Split `header.tsx` god-component; fix its 60/sec re-render on garden-nav hover | L | do together — same file | TODO |
| — | DX-01 | Add CI: typecheck + unit + e2e on push/PR | M | — | TODO |
| — | DX-02 | Add ESLint (`eslint-config-next`, at minimum `react-hooks/exhaustive-deps`) | S-M | — | TODO |

## P2 — real, lower urgency

| Finding IDs | Title | Effort | Depends on | Status |
|---|---|---|---|---|
| SEC-06 | Add content-type/extension allowlist to jordan upload | S | plan 002 | TODO |
| SEC-07 | Make MCP privacy filter an explicit allowlist, not opt-out | M | — | TODO |
| SEC-04 | Fix `validate-contact`'s spoofable rate-limit header (match the other 6 routes) | S | — | TODO |
| SEC-10 | Stop returning raw exception messages to API clients (~10 routes) | S | — | TODO |
| SEC-11 | Fix visits-worker CORS origin-reflection; add baseline security headers | S | — | TODO |
| CORRECTNESS-03 | Add conflict detection to jordan document/canvas state saves | L | plan 002 | TODO |
| CORRECTNESS-05 | Fix jordan upload daily-quota check-then-act race | S | plan 002 | TODO |
| CORRECTNESS-07 | Add TTL/cap to leaderboard identity hashes | M | — | TODO |
| CORRECTNESS-08 | Check `payment_status` before showing "Payment Successful" | S | — | TODO |
| CORRECTNESS-09 | Flush pending canvas events on unmount, not just on interval | S | plan 002 (same files) | TODO |
| TEST-07 | Add unit tests for Stripe checkout route | S | — | TODO |
| TEST-05 | Add tests for cloud-worker's untested routes/admin surface/ZIP writer | M | — | TODO |
| TEST-06 | Unit-test `validate-contact`'s response parsing + the 4 `auth/*` routes | S | — | TODO |
| TEST-08 | Add route-handler tests for the 6 leaderboard endpoints; dedupe IP-parsing | S-M | — | TODO |
| DEBT-04, DOCS-01 | Refresh `.claude/CLAUDE.md`'s state-management + project-structure sections | S | — | TODO |
| DEBT-05 | Split `article-inventory.tsx` into `garden/collectibles/*` | M | — | TODO |
| DEBT-02, DEP-05 | Dedupe the two MSDF build scripts; drop the orphaned one + its 130MB deps | S | — | TODO |
| PERF-01 | Add an idle short-circuit to the gold-infection effect | S | — | TODO |
| PERF-03 | Move static About content out of the root client boundary | M | — | TODO |
| DEP-01 | Pin Stripe `apiVersion`; consolidate the 2 client instantiations into 1 | S | — | TODO |
| DEP-03 | Re-align wrangler/workers-types versions across the 3 workers | S | — | TODO |
| DEP-04 | Bump root TypeScript off its Angular-era pin | S | — | TODO |
| DX-04 | Add root `.env.example` documenting all 18 vars | S | — | TODO |

## P3 / investigate (low priority)

| Finding IDs | Title | Status |
|---|---|---|
| SEC-13 | "Taken" article gate is a hardcoded client-side password (reads as an intentional easter egg) | TODO |
| SEC-12 | HIGH lodash advisory, unreachable — resolved once DEP-05's script cleanup lands | TODO |
| CORRECTNESS-10 | `TextNode` never resyncs if content changes externally (latent until CORRECTNESS-03 ships) | TODO |
| CORRECTNESS-11 | Checkout amount validation doesn't reject non-finite input (Stripe is the real backstop) | TODO |
| CORRECTNESS-12 | cloud-worker cache-invalidation key ignores subpath (likely unreachable today) | TODO |
| TEST-04 | Fix jordan page-title metadata leak (`title: { absolute: ... }`) | TODO |
| TEST-09 | Add a test script + first smoke test to `visits-worker` | TODO |
| TEST-10 | Port `gem-rain`'s 3 real-timer tests to the `page.clock` pattern already proven in the same file | TODO |
| TEST-11 | Investigate whether `downloads.spec.ts`'s rate-limit test undercounts due to shared external Upstash state | TODO |
| DEBT-07 | Decide: link `/taste-prototype` somewhere real, or delete it | TODO |
| DEBT-09 | Consider modeling `garden-explorer.tsx`'s 10-boolean tab transition as one state machine | TODO |
| DEBT-10 | Extract a shared `requireBearer()` helper (duplicated 4x across 2 workers) | TODO |
| PERF-04 | Parallelize/pipeline the garden view-count route's 2 sequential Redis calls | TODO |
| PERF-05 | Delete the dead `narrative-deep-dive.tsx` three.js component, or re-wire it via `next/dynamic` | TODO |
| PERF-06 | Move popout-drag position tracking to a ref, commit to state only on mouseup | TODO |
| PERF-07 | Investigate whether the Unicorn Studio hero scene needs visibility/reduced-motion gating | TODO |
| DEP-06 | Document why `@modelcontextprotocol/sdk` is exact-pinned (matches `agents`' internal pin) | TODO |

## Findings considered and rejected / verified clean (do not re-audit)

- **"Paper redesign" home-vs-other-routes inconsistency** — doesn't exist. Committed and fully reverted same day (2026-06-13, `dde7313`→`ccdaff4`→`2c1b2cf`). See `portfolio-paper-redesign.md` in project memory.
- **`seeking-community-body.tsx`, `garden/page.tsx`** — high commit churn but genuinely simple (narrative prose, decorative SVG). Not complexity problems.
- **`community-nodes.tsx`** — 904 lines, but size is domain-inherent to a tunable particle simulation, not a tangled component.
- **`src/lib/utils.ts`** — 14 importers, but four genuinely cohesive helpers. Not a junk-drawer.
- **No layering violations** found between `src/lib`, `src/context`, `src/components`, `src/app`.
- **Cross-worker D1/CORS duplication** mostly didn't hold up on inspection — only 2 of 3 workers use D1, for different schemas; CORS is hand-rolled in exactly one place. (The one real duplication, the bearer-check, is DEBT-10 above.)
- **`dangerouslySetInnerHTML`** usage confirmed scoped to static, author-controlled `about.json` content only, matching the stated convention. No action needed.
- **Stripe checkout has no webhook** anywhere in the repo, and the client-supplied amount is an intentional pay-what-you-want design, not a manipulable fixed price. No action needed.
- **`mcp-worker`'s vitest suite** — 27/27 passing, includes a real privacy-filter test. Best-tested surface in the repo.
- **Baseline verified clean at audit time**: `npx tsc --noEmit` (0 errors), `bun test src` (19/19 pass), `bun run mcp:check` (in sync), no secrets ever committed to `.env.local`'s git history.

## Direction — separate track, not prioritized against the bugs above

Do not start any of these without checking with Mannan first — priority/taste calls, not engineering judgment calls.

1. **Ship "Drops"** (inbound file-sharing) — fully designed across `docs/superpowers/plans/2026-06-30-drops-m1-collect.md` and related docs; zero code exists yet.
2. **Jordan CRUD gaps** — every jordan resource can be created/read/updated but the only delete is the reset route's total wipe; versions already store full snapshots, restore is mostly reusing the existing write path.
3. **Garden articles → Markdown/MDX** — the repo already proves this pattern for episodes (`readFileSync` + `ReactMarkdown`); garden articles are still hand-coded TSX per piece.
4. **MCP "Publisher Intent" layer** — a completed research spike (`research/mcp-publisher-intent-proof-spike.md`) already designed a data model + 3-phase roadmap; none of it is implemented.
5. **Reuse the leaderboard's lightweight-identity primitive** (`src/lib/leaderboard-store.ts`'s cookie + magic-email pattern) for any future feature that needs no-signup identity.
6. **Chicken game escape-vehicles escalation** — fully spec'd in `tasks/task-244.md` + `docs/chicken-game-escape-scenes-raw.md`, 0-of-7 built.
